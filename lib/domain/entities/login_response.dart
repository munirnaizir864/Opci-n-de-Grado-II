import 'user_entity.dart';

class LoginResponse {
  // final String token;
  // final UserEntity user;

  // LoginResponse(this.token, this.user);
  final int id;
  final bool response;

  LoginResponse(this.id, this.response);
}
